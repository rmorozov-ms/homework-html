export default class ContrAgent {
    constructor(id, name, address, inn, kpp) {
        this.id = id
        this.name = name
        this.address = address
        this.inn = inn
        this.kpp = kpp
    }
}